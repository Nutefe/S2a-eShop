<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.esgis.s2aeshop">

    <application
        android:hardwareAccelerated="false"
        android:largeHeap="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ActivityLogin"
            android:label="@string/app_name"
            android:theme="@style/AppThemes">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ChangePassword"/>

        <activity android:name=".MainActivity"
            android:theme="@style/AppThemes"
            android:windowSoftInputMode="adjustPan"/>

        <activity android:name=".ActivityTypeCompte"/>

        <activity android:name=".ActivityForgetPassword"/>

        <activity android:name=".ActivityCreateSimpleCompte"/>

        <activity android:name=".ActivityCreateFournisseurCompte1"/>

    </application>

</manifest>